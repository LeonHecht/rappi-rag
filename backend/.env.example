APP_ENV=local
API_VERSION=v1
PORT=8000
ALLOWED_ORIGINS=http://localhost:5173

# Set DEMO_MODE=true for a local hand-in that requires no Supabase token.
DEMO_MODE=false
AUTH_DISABLED=false
DEMO_USER_EMAIL=demo@example.com
DEMO_PERSONAL_SPACE=personal

# bm25 is easiest for local development. Use opensearch when a cluster is running.
SEARCH_BACKEND=bm25
CORPUS_PATH=data/static_corpus
DATA_UPLOAD=backend/app/api/data/user_uploads

OPENSEARCH_HOSTS=http://localhost:9200
OPENSEARCH_VERIFY_CERTS=false
OPENSEARCH_INDEX_PREFIX=rag-template

OPENAI_API_KEY=
OPENAI_CHAT_MODEL=gpt-5-nano

SUPABASE_URL=
SUPABASE_SECRET_KEY=
# Optional override. When blank, the backend derives:
# ${SUPABASE_URL}/auth/v1/.well-known/jwks.json
SUPABASE_JWKS_URL=

STRIPE_SECRET_KEY=
BILLING_RETURN_URL=http://localhost:5173/settings/billing
FRONTEND_BASE_URL=http://localhost:5173
